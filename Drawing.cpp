#include "Drawing.h"

void drawing(sf::RenderWindow* window, GameField* gameField, Caption* text)
{
	//Отрисовка поля (границы, фигуры)
	gameField->drawField(window);

	//Отрисовка интерфейса

}